spring:
  datasource:
    url: jdbc:h2:mem:testing-grounds
    username: sa
    password: pword
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true